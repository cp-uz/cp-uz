export type BookmarkEntry = { id: number | string; articleSlug: string };
export type ProgressEntry = { id: number | string; articleSlug: string; percent: number; status: string };
export type NoteEntry = { id: number | string; articleSlug: string; body: string; anchor: string };
